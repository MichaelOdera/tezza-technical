# Copy the encrypted properties into the matching service file after generating
# ciphertext with POST /encrypt. Do not commit plaintext values or the encryption key.
spring:
  datasource:
    password: "{cipher}REPLACE_WITH_CIPHERTEXT_FROM_CONFIG_SERVER"