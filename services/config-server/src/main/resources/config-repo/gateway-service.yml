server:
  port: ${GATEWAY_SERVICE_PORT:8080}
spring:
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
services:
  product-url: ${PRODUCT_SERVICE_URL:http://localhost:8081}
  customer-url: ${CUSTOMER_SERVICE_URL:http://localhost:8082}
  loan-url: ${LOAN_SERVICE_URL:http://localhost:8083}
  notification-url: ${NOTIFICATION_SERVICE_URL:http://localhost:8084}